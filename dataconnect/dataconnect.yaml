specVersion: "v1beta"
serviceId: "personnality"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "personnality-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
